[package]
name = "otspec"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
shrinkwraprs = "0.3.0"
otspec_macros = { path = "../otspec_macros" }
chrono = { version = "0.4.3" }
